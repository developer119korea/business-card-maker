import React from 'react';
import styles from './preview.module.css';

const Preview = (props) => (
  <section className={styles.peview}>
    <h1 className={styles.title}>Preview</h1>
  </section>
);

export default Preview;